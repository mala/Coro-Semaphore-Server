This is Perl module Coro::Semaphore::Server.

INSTALLATION

Coro::Semaphore::Server installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Coro::Semaphore::Server

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Coro::Semaphore::Server documentation is available as in POD. So you can do:

    % perldoc Coro::Semaphore::Server

to read the documentation online with your favorite pager.

mala
